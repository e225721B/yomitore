# ヨミトレ（Yomitore） プロダクト仕様書

読書好きの「気になる」を、良いタイミングで届ける情報アグリゲーター
— サーバーサイド / インフラエンジニア ポートフォリオ —

作成日: 2026年7月
※プロダクト名「ヨミトレ」は仮称です。

## 1. エグゼクティブサマリー

本についての話題（紹介動画・レビュー・トレンド）は YouTube や SNS に散在しており、読者が自分から探しに行かない限り届かない。その結果、購入・読書のベストタイミングを逃してしまう。

ヨミトレは、ユーザーが登録した「気になる本」や「興味分野」をもとに、外部プラットフォームの話題を自動で収集・集約し、良いタイミングで届けるサービスである。読書管理（トラッキング）の飽和市場ではなく、"本に関する情報の散在" という未解決の課題を対象とする。

技術的には「収集 → 集約 → 配信」のデータパイプラインとして設計し、AWS（Amazon EKS を中核）上に構築する。

## 2. 背景と原体験

既存の読書管理アプリ（国内シェア上位の読書メーター等）を日常的に利用する中で、以下の不満を感じたことが出発点である。

- 動作が遅い
- 読書好き同士の交流がコメント・いいねに限られ、本の魅力を発信する手段が乏しい
- ネタバレを気にして具体的な感想を書きづらい
- YouTube のように、今流行っている本のトピックを流し見できる場がない

これらを掘り下げた結果、根本にあるのは「本に関する良質な情報が各所に散らばっていて、自分に届かない」という構造的な課題だと結論づけた。

## 3. 課題定義

本好きは「気になる本」に出会っても、その熱量を保ったまま情報を追い続ける手段がない。本の話題は YouTube や SNS に散在し、自分から探しに行かないと届かないため、購入・読書のベストタイミングを逃す。

この「情報が向こうから来ない」課題を、外部データの収集・集約・配信基盤で解決する。

この課題設定の意図は、UX や企画の問題をインフラ・バックエンドの問題に翻訳している点にある。「散在する情報を集め、個人に最適な形で届ける」ことは、データパイプラインとサーバーサイド設計の課題そのものである。

## 4. 競合分析とポジショニング

読書関連アプリ市場を「本を記録・管理する軸」と「本の話題・情報を発見する軸」、および「データソースが自前か外部集約か」で整理した。

|              | 本を「記録・管理」する | 本の「話題・情報を発見」する |
|---|---|---|
| **自前DB / ユーザー投稿が主** | Goodreads（最大手 / Amazon系）、読書メーター | StoryGraph（気分・ペースで推薦）、Fable / Basmo |
| **外部SNS / 動画を集約する** | Book Notification（新刊メール通知） | ★空白領域★ ヨミトレ（興味 × 外部話題を自動集約・配信） |

- 読書管理・トラッキングは飽和市場であり、StoryGraph 等が強い（Goodreads 代替として 2026年1月に 500万ユーザー到達）。この領域では正面から戦わない。
- 一方、SNS 上の本の話題（TikTok の #BookTok、Instagram の #Bookstagram）は巨大な現象だが、各プラットフォーム内に閉じており、「興味に合わせて横断的に集約して届ける」存在がない。
- Book Notification 等の新刊通知サービスは出版データベース起点であり、SNS・動画コンテンツの集約とは別物。

**結論**: ヨミトレの核心「外部の話題を、興味ベースで集約して届ける」は既存アプリの死角にある。競合が存在すること自体が需要の証明であり、その中で空白のポジションを取れる。

## 5. ターゲットユーザー

- 本は好きだが、能動的に情報収集する時間が取りにくい人
- BookTube / BookTok などの動画コンテンツで本を知るのが好きな人
- 「積読候補」を抱えており、買うタイミングのきっかけが欲しい人

## 6. 機能要件（MVP スコープ）

就活プロダクトとして「完成してデプロイできる」ことを最優先し、スコープを厳格に仕分けた。

### Must（MVP 必須）

| ID | 機能 | 概要 |
|---|---|---|
| M1 | 追跡対象の登録 | 「気になる本」と「興味分野」を登録（両者を共通の "追跡対象" として扱う） |
| M2 | 外部コンテンツの自動収集 | YouTube 等から関連動画・話題を定期バッチで収集 |
| M3 | マッチング | 収集コンテンツ × 追跡対象 を AI（埋め込み）で関連づけ |
| M4 | 新着一覧表示 | 追跡対象に関する新着コンテンツをアプリ内で表示 |
| M5 | トレンド表示 | 本・トピック横断で「今盛り上がっているもの」を表示 |

### Should（余裕があれば）

- S1: AI による動画の内容要約・ネタバレ度タグ付け
- S2: プッシュ通知（新着・購入おすすめタイミング）

### Won't（今回はやらない / 将来拡張）

- W1: Instagram 等 SNS の収集（公式 API の制約が厳しいため YouTube 中心で開始）
- W2: リアルタイム交流機能（読書はマイペースが本質というドメイン判断により意図的に除外）

**設計上の判断メモ**: 「本」と「興味分野」を別実装にせず、共通の "追跡対象" として抽象化した。これによりマッチング処理を1系統に保ちつつ、新規ユーザーのコールドスタート問題（登録した本がまだ無い状態）も解消する。

## 7. 技術選定

| レイヤ | 採用技術 | 選定理由 |
|---|---|---|
| フロントエンド | TypeScript + Next.js | 実務経験がある強み。SSR でトレンド表示の初期表示を高速化 |
| 配信 | CloudFront + S3 | 静的配信・キャッシュで応答を高速化 |
| API サーバー | Node.js (TypeScript) on EKS | フロントと言語を統一し開発効率を確保 |
| 収集ワーカー | Python on EKS（CronJob） | データ処理に慣れた言語。定期バッチで外部 API を収集 |
| 非同期基盤 | Amazon SQS | 収集タスクのバッファリングとバースト吸収 |
| マッチング/AI | Python + 埋め込みベクトル | 本・興味と動画の意味的マッチング |
| データベース | RDS (PostgreSQL) + pgvector | 構造化データとベクトル検索を単一 DB に集約し運用を単純化 |
| キャッシュ | ElastiCache (Redis) | トレンド集計結果をキャッシュし高速応答 |
| オーケストレーション | Amazon EKS (Kubernetes) | 異なる負荷特性のワーカーを独立にスケール |
| IaC | Terraform | インフラをコード化（インフラ職としての証明） |
| CI/CD | GitHub Actions → ECR → EKS | コンテナの自動ビルド・デプロイ |
| 監視 | Prometheus + Grafana / CloudWatch | k8s 上の稼働を可視化 |

### なぜ Kubernetes (EKS) か（本プロジェクトの核）

収集・集計・AI マッチングは、それぞれ負荷特性が異なる。

- 収集ワーカー: 定期実行（CronJob）
- 集計ワーカー: 負荷に応じて水平スケール（HPA）
- AI 埋め込みワーカー: 実行時間が長く重い処理を独立してスケール

これらを「異種ワーカーの定期実行 + 自動スケール + 分離」として素直に表現できるのが Kubernetes を選ぶ理由である。特に AI 埋め込みは実行時間が長いため、実行時間に制約のある Lambda より EKS が適していると判断した。

## 8. アーキテクチャ構成

```
        ┌─────────────────────┐
        │   ユーザー / ブラウザ   │
        └──────────┬──────────┘
                   ↓
        ┌─────────────────────┐
        │  Next.js フロント       │  CloudFront + S3
        └──────────┬──────────┘
                   ↓
  ┌──────────── Amazon EKS クラスタ ────────────┐
  │                                            │
  │  ┌──────────┐   ┌──────────┐   ┌────────┐ │
  │  │ API       │   │ 収集ワーカー │──▶│YouTube │ │
  │  │ Node/TS   │   │ Python    │   │ API    │ │
  │  └──────────┘   └────┬─────┘   └────────┘ │
  │         ┌────────────┴─────┐               │
  │         │ AI マッチワーカー    │               │
  │         │ 埋め込み生成・照合    │               │
  │         └──────────────────┘               │
  └───────────────────┬────────────────────────┘
                      ↓
  ┌──────────── マネージドデータ層 ──────────────┐
  │  ┌────────────┐ ┌──────────┐ ┌──────────┐ │
  │  │ RDS         │ │ElastiCache│ │ SQS       │ │
  │  │ PostgreSQL  │ │ Redis     │ │ 収集キュー  │ │
  │  │ + pgvector  │ │ トレンド集計│ │           │ │
  │  └────────────┘ └──────────┘ └──────────┘ │
  └──────────────────────────────────────────┘
```

## 9. データフロー（収集 → 集約 → 配信）

1. **収集**: 収集ワーカー（CronJob）が定期的に YouTube API を叩き、本・興味に関する動画メタデータを取得する。取得タスクは SQS に積まれ、バーストを吸収する。
2. **集約**: AI マッチワーカーが SQS からタスクを取り出し、動画内容を埋め込みベクトル化。ユーザーの追跡対象（ウォッチリスト）と RDS（pgvector）上で意味的にマッチングし、結果を保存する。
3. **集計**: トレンド（言及数・動画数の増加）を集計し、ElastiCache にキャッシュする。
4. **配信**: フロントの API サーバーはキャッシュを読むだけで応答するため高速。これが「既存アプリの遅さ」への直接的な回答となる。
