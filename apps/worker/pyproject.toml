[project]
name = "yomitore-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "psycopg[binary]>=3.2",
    "requests>=2.32",
    "boto3>=1.35",
    "fastembed>=0.4",
    "redis>=5.0",
]
